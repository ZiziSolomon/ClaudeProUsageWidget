# Build and development dependencies.
# Install with:  pip install -r requirements-dev.txt
# (Also install runtime deps:  pip install -r requirements.txt)

# Package the widget into a standalone .exe (Windows)
pyinstaller>=6.0.0

# Test runner
pytest>=8.0.0
